{
  "name": "issue-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && node dist/index.js"
  }
}
```

Press `Ctrl+S`. Then run:
```
cd C:\Users\HP\issue-tracker
git add .
git commit -m "fix package.json"
git push
```

Then:
```
cd backend
railway up